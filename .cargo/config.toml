[alias]
build-bridge = "build -p termux-bridge"
build-musl = "build -p termux-bridge --target aarch64-unknown-linux-musl --release"
check-musl = "check -p termux-bridge --target aarch64-unknown-linux-musl"

[target.aarch64-unknown-linux-musl]
rustflags = ["-C", "target-feature=+crt-static"]
