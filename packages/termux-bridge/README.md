# @obsidian-termux/termux-bridge

이 패키지는 향후 `Termux` 안에서 실행될 로컬 브리지 프로세스를 위한 자리다.

초기 단계에서는 패키지 경계만 정의한다.

- 예상 역할: `localhost:11557` 계열 엔드포인트 제공
- 예상 책임: 세션 attach, 상태 확인, 재접속 보조, 리사이즈 전달
- 현재 상태: 실행 로직 없음
