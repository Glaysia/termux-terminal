# Marketplace Compliance Plan

1. Correct manifest metadata and move settings to Obsidian's declarative API.
2. Preserve terminal leaves through plugin unload and mark command promises ignored.
3. Split bridge and plugin release assets, then attest both build outputs.
4. Build and run release metadata validation before committing to `dev`.
